<header>
    Page Header

    <!-- <ul>
        <% if(locals.user){ %>
            <li>
                <%= user.name %>
            </li>
        <% }else{ %>

        <% } %>
    </ul> -->
</header>
